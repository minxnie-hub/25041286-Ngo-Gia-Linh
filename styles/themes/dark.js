import colors from '../colors';
import base from './base';

export default {
  ...base,
  mode: 'dark',
  text: colors.white,
  background: colors.black,
  accent: colors.blue,
  onAccent: colors.cream,
};
