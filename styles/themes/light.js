import colors from '../colors';
import base from './base';

export default {
  ...base,
  mode: 'light',
  text: colors.black,
  background: colors.white,
  accent: colors.red,
  onAccent: colors.cream,
};
